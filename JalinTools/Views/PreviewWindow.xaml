<Window x:Class="JalinTools.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Preview Hasil Parse EJ"
        Height="700"
        Width="1200"
        MinHeight="500"
        MinWidth="800"
        Background="#F5F5F5"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#DB4231" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="FileEye" 
                                             Width="28" 
                                             Height="28" 
                                             Foreground="White"
                                             VerticalAlignment="Center"
                                             Margin="0,0,15,0"/>
                    <TextBlock Text="Preview Hasil Parse EJ" 
                               FontSize="20" 
                               FontWeight="SemiBold" 
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           x:Name="FilePathText"
                           FontSize="13" 
                           Foreground="White"
                           Opacity="0.9"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0,0,20,0"/>

                <Button Grid.Column="2"
                        Content="TUTUP"
                        Click="CloseButton_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        BorderBrush="White"
                        Foreground="White"
                        Height="36"
                        Padding="20,0"/>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="1" 
                Background="White"
                Margin="20"
                CornerRadius="8"
                BorderBrush="#E0E0E0"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <TextBox x:Name="ContentTextBox"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         FontSize="12"
                         Padding="20"
                         Background="Transparent"
                         BorderThickness="0"
                         VerticalScrollBarVisibility="Disabled"
                         HorizontalScrollBarVisibility="Disabled"/>
            </ScrollViewer>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" 
                Background="#FAFAFA"
                Padding="20,15"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Information" 
                                             Width="18" 
                                             Height="18" 
                                             Foreground="#757575"
                                             VerticalAlignment="Center"
                                             Margin="0,0,10,0"/>
                    <TextBlock x:Name="LineCountText"
                               FontSize="13" 
                               Foreground="#757575"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="COPY SEMUA"
                            Click="CopyButton_Click"
                            Height="36"
                            Padding="20,0"
                            Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
                                <Setter Property="BorderBrush" Value="#755DE7"/>
                                <Setter Property="Foreground" Value="#755DE7"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#755DE7"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="BUKA FILE"
                            Click="OpenFileButton_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#DB4231"
                            BorderBrush="#DB4231"
                            Foreground="White"
                            Height="36"
                            Padding="20,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
